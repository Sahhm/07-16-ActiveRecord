<form action="delete_photographer">
  Select Photographe to deleter:(Warning deleting a photographer will delete all his photos)<br>
  <select name="photographer_id">
    <% Photographer.all.each do |pic| %>
      <option value="<%= pic.id %>"><%= pic.name %></option>
    <% end %>
  </select><br>
 
  
  <br>

  <input type="submit">
</form> 